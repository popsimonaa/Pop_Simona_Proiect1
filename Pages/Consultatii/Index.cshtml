@page
@model Pop_Simona_Proiect1.Pages.Consultatii.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Lista tipuri de consultatii</h1>

<p>
    <a asp-page="Create">Adauga un nou tip</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Consultatie[0].TipConsultatie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Consultatie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipConsultatie)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
